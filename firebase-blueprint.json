{
  "entities": {
    "BlogPost": {
      "title": "Blog Post",
      "description": "A blog post entry for the website.",
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "The title of the blog post."
        },
        "slug": {
          "type": "string",
          "description": "The URL-friendly identifier for the post."
        },
        "excerpt": {
          "type": "string",
          "description": "A short summary of the post."
        },
        "content": {
          "type": "string",
          "description": "The full content of the blog post."
        },
        "image": {
          "type": "string",
          "format": "uri",
          "description": "The URL of the cover image."
        },
        "color": {
          "type": "string",
          "description": "The Tailwind color class for the post card (e.g., bg-brand-cream text-brand-dark)."
        },
        "date": {
          "type": "string",
          "description": "The formatted date string (e.g., 12 de Marzo, 2026)."
        },
        "author": {
          "type": "string",
          "description": "The author of the post."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the post was created."
        }
      },
      "required": ["title", "slug", "excerpt", "content", "image", "color", "date", "author", "createdAt"]
    },
    "User": {
      "title": "User",
      "description": "A user of the system, primarily for admin access.",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "role": {
          "type": "string",
          "enum": ["admin"],
          "description": "The role of the user."
        }
      },
      "required": ["email", "role"]
    },
    "Mapa": {
      "title": "Mapa",
      "description": "A Google My Maps embed configuration.",
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "The title of the map."
        },
        "mid": {
          "type": "string",
          "description": "The Google Maps ID (mid parameter)."
        },
        "description": {
          "type": "string",
          "description": "A short description of what the map shows."
        },
        "icon": {
          "type": "string",
          "description": "The name of the Lucide React icon to use."
        },
        "order": {
          "type": "number",
          "description": "The order in which the map should appear."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the map was created."
        }
      },
      "required": ["title", "mid", "description", "icon", "order", "createdAt"]
    }
  },
  "firestore": {
    "blogPosts": {
      "schema": {
        "$ref": "#/entities/BlogPost"
      },
      "description": "Collection of all blog posts."
    },
    "users": {
      "schema": {
        "$ref": "#/entities/User"
      },
      "description": "Collection of users for role-based access control."
    },
    "mapas": {
      "schema": {
        "$ref": "#/entities/Mapa"
      },
      "description": "Collection of Google My Maps configurations."
    }
  }
}
